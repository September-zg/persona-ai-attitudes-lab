# 技术报告

> 本报告按老师要求的 11 个问题组织。试点人数、反馈和用户发现必须使用真实记录填写；未完成部分保留“待填写”，不使用合成数据代替。

## 1. 我构建了什么？

我构建了 **Personality & AI Attitudes Lab**，一个面向教育/研究探索的网页测评原型。参与者先阅读说明并同意参与，再完成五大人格和 AI 态度两部分问卷，提交后看到个人结果，并可选填平台体验反馈。研究者入口显示多名参与者的汇总统计。

公网入口：<https://persona-ai-attitudes-lab.onrender.com/>  
源码仓库：<https://github.com/September-zg/persona-ai-attitudes-lab>  
研究者入口：<https://persona-ai-attitudes-lab.onrender.com/index.html?view=admin>

## 2. 测量了哪些构念？

人格部分采用五个常见维度：

- 外向性：社交活跃和精力外向的一端；
- 宜人性：体谅、合作和亲和的一端；
- 尽责性：条理、坚持和任务投入的一端；
- 神经质：压力、担忧和负性情绪敏感度；
- 开放性：对新想法、抽象概念和想象的兴趣。

人格部分共 20 题，每个维度 4 题。中文题目是项目翻译，不宣称为经过验证的中文版。

态度部分测量对 AI 的三个探索性维度：感知效益、信任与采纳、风险顾虑，共 6 题。这些是项目自编的探索性题目，不宣称为正式量表。

## 3. 为什么选择这些构念？

五大人格提供了一个容易解释、覆盖面较广的描述框架，适合课程要求中的“人格”部分。AI 态度与项目主题直接相关，同时把“觉得有帮助”“是否信任和愿意使用”“是否担心风险”分开，避免把对 AI 的看法压缩成一个没有解释力的总分。两部分只用于描述当前样本，不用于诊断或预测个人行为。

## 4. 计分系统如何工作？

所有题目使用 1–5 分。人格题的选项表示陈述与自己的符合程度；AI 态度题的选项表示同意程度。反向题使用：

```text
反向分 = 6 - 原始分
```

每个维度的分数是该维度题目（先完成反向转换）的平均值，因此范围仍为 1–5。服务端只接受完整回答、整数 1–5，并根据原始 `answers` 重新计算分数，不信任浏览器提交的 `scores`。具体规则固定在 `scoring.py`。

## 5. 使用了哪些技术？

- 前端：原生 HTML、CSS、JavaScript；
- 后端：Python 标准库 `http.server`；
- 存储：服务器端 JSON 文件，浏览器端 `localStorage` 作为离线回退；
- 部署：Render Web Service；
- 版本控制：GitHub；
- 研究者权限：部署环境变量 `ADMIN_TOKEN` 和 `X-Admin-Token` 请求头。

当前是原型架构，尚未接入正式数据库、账户体系、访问审计或长期备份。

## 6. 我如何使用 Agentic AI？

我使用 Codex 将任务拆成测量设计、参与者流程、数据保存、研究者汇总、测试和部署几个部分，让 AI 协助生成和修改前端、Python 服务端、计分逻辑、测试和文档。每次修改后都通过代码阅读、单元测试、边界测试、浏览器流程和 Render 日志检查，而不是直接接受生成结果。

具体的提示词、决策和修正过程记录在 [`AI_DEVELOPMENT_RECORD.md`](AI_DEVELOPMENT_RECORD.md)，完整开发时间线记录在 [`development-log.md`](development-log.md)。

## 7. 从 10 名参与者那里学到了什么？

> 此节必须在真实试点完成后填写。目前不能用合成数据或开发者自测代替。

- 完成提交人数：待填写；
- 未完成或中途退出人数：待填写；
- 完成率：待填写；
- 用户对说明和题目的理解：待填写；
- 用户喜欢的功能：待填写；
- 主要可用性问题：待填写；
- 计分、导航或移动端异常：待填写；
- 重复出现的改进建议：待填写。

详细记录表见 [`PILOT_EVALUATION.md`](PILOT_EVALUATION.md)。

## 8. 遇到了哪些问题？

开发期间遇到的主要问题包括：公网 HTTPS 页面请求 API 的地址不正确、研究者页面在未认证时可能请求汇总、反向题未统一方向会影响 α、样本太少时显示统计量容易造成过度解读，以及反馈按钮重复点击可能产生重复记录。这些问题已经分别通过同源 API、登录入口、统一反向键、10 条样本门槛和提交按钮禁用进行修正。

仍存在的限制是 Render 免费实例可能休眠，服务器使用本地 JSON，且当前令牌保护不是完整账户认证。

## 9. AI 生成代码时犯过什么错误？

AI 初版曾出现或可能导致以下问题：

- 使用不适合公网 HTTPS 的 API 地址；
- 将研究者页面当作普通页面直接加载汇总；
- α 计算混合反向题的原始方向；
- 在小样本下显示 α 和 Pearson 相关；
- 用过于宽泛的总结语句解释样本结果；
- 清除按钮的文字容易让人误以为会删除服务器数据。

这些不是凭猜测保留的“错误清单”，而是通过代码检查、测试、浏览器行为或日志检查后记录的修正点。

## 10. 我如何验证并修正这些错误？

验证命令：

```bash
python3 -m py_compile server.py scoring.py scoring_examples.py seed_synthetic.py
python3 -m unittest discover -s tests
python3 scoring_examples.py
```

当前结果：Python 编译通过；测试套件 18 项全部通过；计分示例输出 `scoring specification checks passed`。测试覆盖正向/反向计分、边界值、缺失题目、非法类型、输入不可变性、服务端重算以及管理员令牌拒绝/接受。

部署层面，Render 页面显示应用提交 `bdc5b16` 为 Live；公网首页可访问，研究者入口先显示登录框，未带令牌的汇总接口返回 401。合成数据带有 `syntheticTest: true`，不计入真实试点。

## 11. 如果再有一周，我会改进什么？

1. 把本地 JSON 迁移到有备份和访问控制的正式数据库；
2. 使用个人账户或角色权限替代共享管理员令牌，并增加访问审计；
3. 核对人格题目的来源、授权和中文翻译，并完善引用；
4. 对 AI 态度题进行内容审查和更充分的题目设计；
5. 增加浏览器自动化测试和移动端无障碍检查；
6. 加入数据保留期限、删除流程和更清晰的研究者数据导出说明；
7. 用更多真实试点数据复核题目理解和异常回答模式，但不把试点当作正式量表验证。
